#include <iostream>
#include <cmath> 
using namespace std;

int main()
{
    string text;
    getline (cin, text);
    for (int i = 0; i < text.length(); i++) {
        std::cout << text[i] << std::endl;
    }

    return 0;
}
