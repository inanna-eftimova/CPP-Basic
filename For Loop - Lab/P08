#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    int min = INT_MAX;
    int max = INT_MIN;
    for (int i = 0; i < n; i++) {
        int num; cin>>num;
        if(num>max){
            max=num;
        }
        if(num<min){
            min=num;
        }
    }
    cout<<"Max number: "<<max<<endl;
    cout<<"Min number: "<<min<<endl;
    return 0;
}
