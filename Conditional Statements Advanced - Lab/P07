#include <iostream>

using namespace std;

int main()
{
   int hours; cin>>hours;
   string day; cin>>day;
   if(day!="Sunday"&& hours>=10 &&hours<=18){
       std::cout << "open" << std::endl;
   }else{
       cout<<"closed";
   }
    return 0;
}
